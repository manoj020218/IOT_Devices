This folder is a separate legacy comparison build for the Loud SOS Siren project.

It was reconstructed from the older pre-speaker-profile firmware shape after the current working build had already moved forward. The main project folder remains the active build and was not changed by this comparison copy.

Build status: `pio run` passes in this folder.
