# JNX-QRU-C3-001

QRunlock by Jenix — BLE/Wi-Fi provisioned smart rim lock.

## Contents

- `ui-packages/qrunlock-mobile/1.0.0`: full routed device app (device list,
  lock control, RF remote setup, activity logs, settings) mounted via
  RemoteProductMount. Built from real TSX source and a Vite build
  pipeline, not hand-authored — see the platform repo's
  `DEVICE_PACKAGE_RUNTIME.md` ("Two ways to build a package") and
  `PWA_APK/apps/web-pwa/src/features/qrunlock/remotePackage/` for the
  authoring copy and build config this artifact was produced from.

## Current Binding

- PID: `JNX-QRU-C3-001`
- UI package: `qrunlock-mobile@1.0.0`
- UI mode: `remote-package`
